import { useEffect, useState } from "react";

const MapleLeafAnimation = ({ message }) => {
  return (
    <div className="maple-leaf-animation">
      🍁 {message}
    </div>
  );
};

export default MapleLeafAnimation;
